void main(List<String> args) {
  // Periksa apakah 'args' tidak kosong
  if (args.isNotEmpty) {
    // Jika 'args' memiliki elemen, cetak elemen-elemen tersebut
    print('Arguments: $args');
  } else {
    // Jika 'args' kosong, cetak pesan default
    print('EWA SUAMI AYAKA.');
  }
}
